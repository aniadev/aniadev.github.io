# pnpm 11 runs an implicit `pnpm install` before every script (verify-deps-before-run),
# which re-triggers ERR_PNPM_IGNORED_BUILDS on native deps in CI. We install + build the
# native binding explicitly in the workflow, so disable the pre-run verification.
verify-deps-before-run=false
